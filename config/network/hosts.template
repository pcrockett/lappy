# Static table lookup for hostnames.
# See hosts(5) for details.
127.0.0.1        localhost
::1              localhost
${PRINTER_IP_ADDRESS}      printer.home
${SCANNER_IP_ADDRESS}      scanner.home
${COOLIFY_IP_ADDRESS}      coolify.home
${HISTER_IP_ADDRESS}       hister.home
